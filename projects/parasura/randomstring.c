/*
The logic for inputChar() simply returns a random number within the ascii range.

inputString() uses a similar random character method within a range to create a
random character of arrays, or string.
*/
